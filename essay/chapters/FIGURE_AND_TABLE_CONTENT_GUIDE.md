# 论文图表内容创建指引

> 创建日期：2026-04-19
> 用途：为论文各章节图表（图片+表格）提供具体内容与创建prompt

---

## 一、第三章图表需求（共6个：4图+2表）

---

### 图3.1 系统功能模块关系图

**位置**：3.1.1节末，"系统功能模块之间的依赖与调用关系如图3.1所示"后

**图片标题**：图3.1 系统功能模块关系图

**图片类型**：流程图/框图（可用Visio、Draw.io、PowerPoint创建）

**图片内容描述**：

```
四个功能模块的层级关系与调用流向：

┌─────────────────────────────────────────────────────┐
│                  用户管理与历史记录                    │
│         （用户认证、会话管理、历史存储）               │
└─────────────────────────────────────────────────────┘
                          ↑ 提供用户上下文
                          │
┌─────────────────────────────────────────────────────┐
│                IDS规则生成（核心模块）                 │
│      （自然语言解析 → 知识库映射 → IDS输出）          │
└─────────────────────────────────────────────────────┘
          ↓ 调用                    ↓ 输出IDS
          │                         │
┌───────────────────┐     ┌───────────────────┐
│  IFC知识库检索     │     │  合规审查执行      │
│ （向量检索、实体  │     │ （ifctester调用、 │
│  定义查询）        │     │  报告生成）        │
└───────────────────┘     └───────────────────┘
```

**关系说明**：
- IDS生成模块调用知识库检索模块获取IFC实体映射
- IDS生成模块输出规则文件供审查执行模块使用
- 用户管理模块为所有模块提供用户上下文

**创建prompt**：
```
请生成一张学术风格的系统功能模块关系图，用于计算机专业毕业论文。

【整体风格】
- 扁平化设计，简洁专业，白色背景
- 学术论文配图风格，清晰易读
- 图片尺寸：宽度15cm，高度10cm，分辨率300dpi
- 所有文字使用中文，字体清晰

【图表内容】
绘制四个功能模块的层级关系图，从上到下排列：

1. 顶部模块：
   - 横向矩形框，浅灰色填充（#F5F5F5），黑色细边框（1px）
   - 内部居中显示中文"用户管理与历史记录"（黑色，16pt，加粗）
   - 下方小字标注"用户认证、会话管理、历史存储"（深灰色，10pt）

2. 中间核心模块（最重要，需突出显示）：
   - 较大的矩形框，浅蓝色填充（#E3F2FD），深蓝色粗边框（2.5px）
   - 内部居中显示中文"IDS规则生成（核心模块）"（黑色，18pt，加粗）
   - 下方小字标注"自然语言解析 → 知识库映射 → IDS输出"（深灰色，10pt）

3. 底部左侧模块：
   - 矩形框，浅绿色填充（#E8F5E9），黑色细边框（1px）
   - 显示"IFC知识库检索"（黑色，16pt，加粗）
   - 下方小字"向量检索、实体定义查询"（深灰色，10pt）

4. 底部右侧模块：
   - 矩形框，浅橙色填充（#FFF3E0），黑色细边框（1px）
   - 显示"合规审查执行"（黑色，16pt，加粗）
   - 下方小字"ifctester调用、报告生成"（深灰色，10pt）

【连接关系】
- 从顶部模块到中间模块：向下的黑色实线箭头（2px粗），箭头旁边标注"提供用户上下文"（12pt）
- 从中间模块到左下模块：斜向左下的黑色实线箭头（2px粗），标注"调用"（12pt）
- 从中间模块到右下模块：斜向右下的黑色实线箭头（2px粗），标注"输出IDS"（12pt）

【布局要求】
- 顶部模块居中，宽度占画面的70%
- 中间核心模块居中，宽度占画面的80%，高度比其他模块高出20%
- 底部两个模块左右对称分布，各占画面35%宽度，中间留有间隙
- 模块之间垂直间距均匀，约为2cm
- 整体布局平衡，视觉重心在中间的核心模块

【编号标注】
在每个模块的左上角添加圆形编号标记：①②③④（白色圆形背景，黑色数字）
```

---

### 图3.2 系统整体架构图

**位置**：3.2.1节末，"系统整体架构如图3.2所示"后

**图片标题**：图3.2 系统整体架构图

**图片类型**：三层架构图（层级图）

**图片内容描述**：

```
三层架构结构示意：

┌─────────────────────────────────────────────────────────┐
│                    前端交互层                             │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐   │
│  │ 登录页面  │ │IDS生成页 │ │知识库页  │ │审查页面  │   │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘   │
│                  Next.js + React + TypeScript           │
└─────────────────────────────────────────────────────────┘
                          ↓ HTTP RESTful API
┌─────────────────────────────────────────────────────────┐
│                    后端服务层                             │
│  ┌──────────────────┐       ┌────────────────────┐     │
│  │ Next.js API Routes│ ───→ │ Python FastAPI     │     │
│  │ （认证、历史查询） │       │ （IDS生成、审查）  │     │
│  └──────────────────┘       └────────────────────┘     │
└─────────────────────────────────────────────────────────┘
                          ↓ MongoDB Driver
┌─────────────────────────────────────────────────────────┐
│                    数据存储层                             │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐   │
│  │  users   │ │history_ids│ │history_  │ │  GridFS  │   │
│  │  集合    │ │   集合    │ │reviews   │ │  文件    │   │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘   │
│                       MongoDB                            │
└─────────────────────────────────────────────────────────┘
```

**创建prompt**：
```
请生成一张三层系统架构图，用于计算机专业毕业论文，展示前后端分离的Web应用架构。

【整体风格】
- 学术论文配图风格，扁平化设计，白色背景
- 图片尺寸：宽度16cm，高度12cm，分辨率300dpi
- 所有文字使用中文，清晰可读

【三层架构布局】
从上到下绘制三个大的横向矩形区域，每层有不同的背景色：

**第一层：前端交互层（顶部）**
- 整层背景：浅蓝色（#E3F2FD）
- 左上角标注"前端交互层"（黑色，18pt，加粗）
- 包含4个圆角矩形组件，横向排列，白色填充，蓝色边框（1.5px）：
  1. "登录页面"（14pt）
  2. "IDS生成页"（14pt）
  3. "知识库页"（14pt）
  4. "审查页面"（14pt）
- 层底部居中标注技术栈："Next.js + React + TypeScript"（深灰色，12pt，斜体）

**第二层：后端服务层（中间）**
- 整层背景：浅绿色（#E8F5E9）
- 左上角标注"后端服务层"（黑色，18pt，加粗）
- 包含2个圆角矩形组件，横向排列：
  1. 左侧："Next.js API Routes"（白色填充，绿色边框，14pt）
     下方小字："认证、历史查询"（10pt，深灰色）
  2. 右侧："Python FastAPI"（白色填充，绿色边框，14pt）
     下方小字："IDS生成、审查"（10pt，深灰色）
- 两个组件之间用黑色实线箭头连接，标注"调用"
- 层底部居中标注技术栈："FastAPI + LangChain + ifcopenshell"（深灰色，12pt，斜体）

**第三层：数据存储层（底部）**
- 整层背景：浅橙色（#FFF3E0）
- 左上角标注"数据存储层"（黑色，18pt，加粗）
- 包含4个圆角矩形组件，横向排列，白色填充，橙色边框（1.5px）：
  1. "users 集合"（14pt）
  2. "history_ids 集合"（14pt）
  3. "history_reviews 集合"（14pt）
  4. "GridFS 文件存储"（14pt，用虚线边框区分）
- 层底部居中标注技术栈："MongoDB + GridFS"（深灰色，12pt，斜体）

【层间连接】
- 第一层到第二层：粗黑色向下箭头（3px），居中位置，旁边标注"HTTP RESTful API"（12pt）
- 第二层到第三层：粗黑色向下箭头（3px），居中位置，旁边标注"MongoDB Driver"（12pt）

【布局细节】
- 三层高度比例约为 3:3:2.5
- 每层内的组件均匀分布，左右留白
- 层与层之间间距约1.5cm
- 整体居中对齐，视觉平衡
```

---

### 图3.3 前后端交互序列图

**位置**：3.2.3节末，"前后端交互的完整流程如图3.3所示"后

**图片标题**：图3.3 IDS生成请求前后端交互序列图

**图片类型**：UML序列图

**图片内容描述**：

```
时序交互流程：

用户          Next.js前端      API Routes       FastAPI         MongoDB
 │               │                │               │               │
 │──输入规范描述─→│                │               │               │
 │               │──POST /api/ids─→│               │               │
 │               │                │──转发请求─────→│               │
 │               │                │               │──LLM调用      │
 │               │                │               │←─LLM响应───── │
 │               │                │               │──向量检索────→│
 │               │                │               │←─检索结果────│
 │               │                │               │──构建IDS JSON │
 │               │                │←─返回结果─────│               │
 │               │──渲染结果页面──│               │               │
 │←─展示IDS规则─│                │               │               │
 │               │                │               │               │
```

**创建prompt**：
```
请生成一张UML序列图，展示IDS生成请求的前后端交互流程，用于计算机专业毕业论文。

【整体风格】
- 标准UML序列图风格，学术论文配图
- 白色背景，清晰的时间线和消息流
- 图片尺寸：宽度14cm，高度12cm，分辨率300dpi
- 所有文字使用中文

【参与者（从左到右排列）】
在图的顶部绘制5个参与者框，横向均匀分布：

1. "用户"（浅灰色矩形框，#F5F5F5，黑色边框，14pt加粗）
2. "Next.js前端"（浅蓝色矩形框，#E3F2FD，黑色边框，14pt加粗）
3. "API Routes"（浅绿色矩形框，#E8F5E9，黑色边框，14pt加粗）
4. "FastAPI"（浅紫色矩形框，#F3E5F5，黑色边框，14pt加粗）
5. "MongoDB"（浅橙色矩形框，#FFF3E0，黑色边框，14pt加粗）

每个参与者框下方绘制一条垂直虚线（生命线），贯穿整个时序图。

【交互消息（从上到下按时间顺序）】
使用水平箭头表示消息传递，编号标注每个步骤：

① 用户 → Next.js前端：黑色实线箭头，标注"输入规范描述"（12pt）

② Next.js前端 → API Routes：黑色实线箭头，标注"POST /api/ids"（12pt）

③ API Routes → FastAPI：黑色实线箭头，标注"转发请求"（12pt）

④ FastAPI内部处理：在FastAPI生命线上绘制一个浅紫色矩形激活框，标注"LLM调用"（10pt）
   右侧添加注释框（虚线边框）："调用外部LLM服务"

⑤ FastAPI → MongoDB：黑色实线箭头，标注"向量检索"（12pt）

⑥ MongoDB → FastAPI：黑色虚线箭头（返回消息），标注"检索结果"（12pt）

⑦ FastAPI内部处理：在FastAPI生命线上绘制激活框，标注"构建IDS JSON"（10pt）

⑧ FastAPI → API Routes：黑色虚线箭头（返回消息），标注"返回结果"（12pt）

⑨ API Routes → Next.js前端：黑色虚线箭头（返回消息），标注"IDS数据"（12pt）

⑩ Next.js前端内部：在前端生命线上绘制激活框，标注"渲染结果页面"（10pt）

⑪ Next.js前端 → 用户：黑色实线箭头，标注"展示IDS规则"（12pt）

【视觉规范】
- 请求消息：黑色实线箭头，箭头头部为实心三角形
- 返回消息：黑色虚线箭头，箭头头部为实心三角形
- 激活框：浅色矩形，覆盖在生命线上，表示对象处于活动状态
- 步骤编号：圆形白色背景，黑色数字（①②③...），放在箭头起点左侧
- 消息标注：黑色文字，12pt，位于箭头上方
- 注释框：虚线矩形，浅黄色填充（#FFFDE7），用于补充说明

【布局要求】
- 参与者框宽度约2.5cm，高度1cm
- 生命线垂直虚线，灰色，1px
- 消息箭头水平，间距均匀（约0.8cm）
- 整体从上到下流动，时间线清晰
```

---

### 图3.4 数据存储结构示意图

**位置**：3.2.4节末，"数据存储结构如图3.4所示"后

**图片标题**：图3.4 MongoDB数据存储结构示意图

**图片类型**：数据结构示意图/ER图简化版

**图片内容描述**：

```
MongoDB数据库结构：

                    ┌─────────────────┐
                    │    MongoDB      │
                    │    Database     │
                    └─────────────────┘
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
┌───────────┐      ┌───────────┐      ┌───────────┐
│  users    │      │history_ids│      │history_   │
│  集合     │      │   集合    │      │reviews集合│
├───────────┤      ├───────────┤      ├───────────┤
│ username  │      │ input     │      │ ifc_file_id│
│ password  │      │ output    │      │ ids_file_id│
│ created_at│      │ version   │      │ report     │
└───────────┘      │ created_at│      │ created_at │
                   └───────────┘      └───────────┘
                           │                  │
                           └──────────────────┤
                                              │
                                   ┌─────────────────┐
                                   │   GridFS        │
                                   ├─────────────────┤
                                   │ files.chunks   │
                                   │ files.files    │
                                   │ (IFC/IDS文件)  │
                                   └─────────────────┘
```

**创建prompt**：
```
请生成一张MongoDB数据存储结构示意图，展示数据库的集合结构和关系，用于计算机专业毕业论文。

【整体风格】
- 数据库结构图风格，类似简化的ER图
- 白色背景，清晰的层级关系
- 图片尺寸：宽度14cm，高度10cm，分辨率300dpi
- 所有文字使用中文

【顶层容器】
在图的顶部中央绘制：
- 大的圆角矩形框，浅蓝色填充（#E3F2FD），深蓝色边框（2px）
- 内部显示"MongoDB 数据库"（黑色，18pt，加粗）
- 下方小字："ids_agent_db"（深灰色，12pt）

【三个普通集合（中间层）】
从顶层容器向下连接三个集合框，横向排列：

**1. users 集合（左侧）**
- 矩形框，白色填充，浅灰色边框（1.5px）
- 顶部标题栏：浅灰色背景（#F5F5F5），显示"users 集合"（14pt，加粗）
- 下方字段列表（左对齐，12pt，深灰色）：
  • username
  • password_hash
  • created_at

**2. history_ids 集合（中间）**
- 矩形框，白色填充，浅绿色边框（1.5px）
- 顶部标题栏：浅绿色背景（#E8F5E9），显示"history_ids 集合"（14pt，加粗）
- 下方字段列表（左对齐，12pt，深灰色）：
  • input
  • output (JSON)
  • version
  • created_at

**3. history_reviews 集合（右侧）**
- 矩形框，白色填充，浅橙色边框（1.5px）
- 顶部标题栏：浅橙色背景（#FFF3E0），显示"history_reviews 集合"（14pt，加粗）
- 下方字段列表（左对齐，12pt，深灰色）：
  • ifc_file_id
  • ids_file_id
  • report
  • created_at

【GridFS存储单元（底部）】
在图的底部中央绘制：
- 矩形框，浅紫色填充（#F3E5F5），紫色虚线边框（2px，虚线样式）
- 顶部标题："GridFS 文件存储"（14pt，加粗）
- 下方两行小字（12pt，深灰色）：
  • files.chunks（分块数据）
  • files.files（文件元数据）
- 再下方标注："存储 IFC/IDS 文件"（10pt，斜体）

【关联关系】
使用箭头和连线表示集合间的关系：

1. MongoDB数据库 → 三个集合：从顶层容器向下分出三条黑色实线，连接到三个集合

2. history_ids集合 → GridFS：黑色虚线箭头，标注"文件ID引用"（10pt）

3. history_reviews集合 → GridFS：黑色虚线箭头，标注"文件ID引用"（10pt）

【布局要求】
- 顶层MongoDB容器居中，宽度约10cm
- 三个集合框横向均匀分布，每个宽度约3.5cm，高度约4cm
- GridFS框居中，宽度约6cm，高度约3cm
- 垂直间距：顶层到集合层约2cm，集合层到GridFS约2cm
- 关联线使用虚线表示逻辑引用关系，实线表示包含关系

【视觉细节】
- 字段列表使用项目符号（•）
- 字段名称使用等宽字体风格
- GridFS使用虚线边框以区别于普通集合
- 整体布局对称，视觉平衡
```

---

### 表3.1 用户角色功能权限表

**位置**：3.1.3节，"各角色的功能权限详见表3.1"后

**表格标题**：表3.1 用户角色功能权限矩阵

**表格结构**：| 角色 | 功能权限 | 操作范围 |

**表格内容**：

| 角色 | 功能权限 | 操作范围 |
|------|----------|----------|
| BIM工程师 | IDS规则生成、IFC知识库检索、合规审查执行、历史记录查询 | 生成个人项目的IDS规则、验证模型合规性、下载审查报告 |
| 审查人员 | IDS规则加载、合规审查执行、审查报告导出、历史记录查询 | 加载项目IDS规则、执行自动化审查、生成合规报告、导出BCF格式报告 |
| 项目管理员 | 用户管理、项目规范库管理、IDS规则模板配置、全量历史追溯 | 管理团队成员、配置审查标准、维护规范模板、追溯所有审查记录 |

**创建prompt**：
```
请创建一个用户角色功能权限表格，用于毕业论文第三章。

表格要求：
- 标题：表3.1 用户角色功能权限矩阵
- 列数：3列（角色、功能权限、操作范围）
- 行数：4行（含表头）
- 角色：BIM工程师、审查人员、项目管理员

内容要点：
1. BIM工程师：核心操作者，拥有全部生成与审查功能，仅限个人项目
2. 审查人员：执行审查职能，可加载已有IDS规则，侧重报告导出
3. 项目管理员：管理角色，负责配置与追溯，权限范围最广

格式要求：
- 三线表格式（顶线、底线、栏目线）
- 表头加粗居中
- 内容左对齐
```

---

### 表3.2 技术选型汇总表

**位置**：3.3节开头，"各层技术选型及理由详见表3.2"后

**表格标题**：表3.2 系统技术选型汇总

**表格结构**：| 技术层 | 技术/工具 | 选择理由 | 核心优势 |

**表格内容**：

| 技术层 | 技术/工具 | 选择理由 | 核心优势 |
|--------|-----------|----------|----------|
| 前端框架 | Next.js | API Routes集成前后端、SSR提升首屏速度 | 全栈开发、生态成熟 |
| 前端语言 | TypeScript | 静态类型检查、降低运行时异常 | 类型安全、接口清晰 |
| 前端样式 | Tailwind CSS | 原子化CSS、快速构建响应式布局 | 开发效率高、无样式膨胀 |
| HTTP库 | Axios | 请求/响应拦截、完善的错误处理 | 超时控制、拦截机制 |
| 后端框架 | FastAPI | 自动生成OpenAPI文档、异步处理 | 高性能、类型验证 |
| LLM框架 | LangChain | 模块化设计、提示词模板管理 | 管道组装、工具集成 |
| IFC解析 | ifcopenshell | 支持IFC2x3/IFC4、实体定义提取 | 开源稳定、版本兼容 |
| 审查引擎 | ifctester | IDS规则加载、自动化检验 | 标准兼容、报告生成 |
| 数据库 | MongoDB | 文档型存储、无需预定义表结构 | 灵活schema、嵌套支持 |
| 文件存储 | GridFS | 突破16MB限制、分块存储大文件 | 流式传输、部分读取 |

**创建prompt**：
```
请创建一个技术选型汇总表格，用于毕业论文第三章。

表格要求：
- 标题：表3.2 系统技术选型汇总
- 列数：4列（技术层、技术/工具、选择理由、核心优势）
- 行数：11行（含表头）
- 分类顺序：前端(4项) → 后端(4项) → 数据存储(2项)

内容要点：
1. 核心优势列使用2-4字概括
2. 选择理由控制在20字以内
3. 按技术层分组排列

格式要求：
- 三线表格式
- 表头加粗
- 可选：同一技术层行合并"技术层"单元格
```

---

## 二、第四章图表需求（预估：3图+3表）

### 图4.1 IDS生成五阶段管道流程图

**位置**：4.1节末，"IDS生成管道的五个阶段如图4.1所示"后

**图片标题**：图4.1 IDS生成五阶段管道流程图

**图片类型**：横向管道流程图

**创建prompt**：
```
请生成一张IDS生成五阶段管道流程图，展示数据处理的流水线过程，用于计算机专业毕业论文。

【整体风格】
- 学术论文配图风格，管道式横向布局
- 白色背景，清晰的数据流向
- 图片尺寸：宽度16cm，高度8cm，分辨率300dpi
- 所有文字使用中文

【五个阶段（从左到右排列）】

**阶段1：结构化解析**
- 圆角矩形框，浅蓝色填充（#E3F2FD），蓝色边框（1.5px）
- 顶部标题："阶段1：结构化解析"（14pt，加粗）
- 中间图标：文档解析图标（简化的文档符号）
- 底部标注："输入：自然语言描述"（10pt，深灰色）
- 底部标注："输出：结构化数据"（10pt，深灰色）

**阶段2：方面分类**
- 圆角矩形框，浅绿色填充（#E8F5E9），绿色边框（1.5px）
- 顶部标题："阶段2：方面分类"（14pt，加粗）
- 中间图标：分类图标（简化的标签符号）
- 底部标注："输入：结构化数据"（10pt，深灰色）
- 底部标注："输出：Facet分类"（10pt，深灰色）

**阶段3：知识库映射（核心阶段，需突出）**
- 圆角矩形框，浅紫色填充（#F3E5F5），紫色粗边框（2.5px）
- 顶部标题："阶段3：知识库映射 ⭐"（16pt，加粗）
- 中间图标：数据库图标（简化的数据库符号）
- 底部标注："输入：Facet分类"（10pt，深灰色）
- 底部标注："输出：IFC实体映射"（10pt，深灰色）
- 框体比其他阶段略大（高度增加15%）

**阶段4：约束提取**
- 圆角矩形框，浅橙色填充（#FFF3E0），橙色边框（1.5px）
- 顶部标题："阶段4：约束提取"（14pt，加粗）
- 中间图标：规则图标（简化的齿轮符号）
- 底部标注："输入：IFC实体映射"（10pt，深灰色）
- 底部标注："输出：约束规则"（10pt，深灰色）

**阶段5：IDS构建**
- 圆角矩形框，浅红色填充（#FFEBEE），红色边框（1.5px）
- 顶部标题："阶段5：IDS构建"（14pt，加粗）
- 中间图标：文件生成图标（简化的文件符号）
- 底部标注："输入：约束规则"（10pt，深灰色）
- 底部标注："输出：IDS文件"（10pt，深灰色）

【连接箭头】
在每两个阶段之间绘制粗黑色实线箭头（3px），箭头头部为实心三角形，表示数据流向：
- 阶段1 → 阶段2
- 阶段2 → 阶段3
- 阶段3 → 阶段4
- 阶段4 → 阶段5

【布局要求】
- 五个阶段框等宽（约2.8cm），高度约5cm（阶段3略高）
- 阶段框之间水平间距约0.8cm
- 箭头长度与间距匹配
- 整体居中对齐，视觉流畅

【视觉细节】
- 阶段编号使用圆形标记（①②③④⑤），放在框的左上角
- 核心阶段（阶段3）使用星号⭐标记
- 输入输出文字使用不同颜色区分（输入：深蓝色，输出：深绿色）
- 图标使用简化的线条风格，黑色或深灰色
```

### 图4.2 IFC知识库向量索引构建流程图

**位置**：4.2节末，"IFC知识库向量索引的构建流程如图4.2所示"后

**图片标题**：图4.2 IFC知识库向量索引构建流程图

**图片类型**：垂直流程图

**创建prompt**：
```
请生成一张IFC知识库向量索引构建流程图，展示从原始文档到向量索引的完整流程，用于计算机专业毕业论文。

【整体风格】
- 学术论文配图风格，垂直流程布局
- 白色背景，清晰的处理流程
- 图片尺寸：宽度12cm，高度14cm，分辨率300dpi
- 所有文字使用中文

【流程步骤（从上到下排列）】

**步骤1：IFC官方文档（数据源）**
- 圆角矩形框，浅灰色填充（#F5F5F5），黑色边框（1.5px）
- 内部显示："IFC官方文档"（16pt，加粗）
- 下方小字："IFC4.3 规范文档"（12pt，深灰色）
- 左侧添加文档图标（简化的多页文档符号）

**步骤2：实体提取**
- 圆角矩形框，浅蓝色填充（#E3F2FD），蓝色边框（1.5px）
- 内部显示："实体提取"（16pt，加粗）
- 下方小字："解析 IfcWall、IfcDoor 等实体定义"（11pt，深灰色）
- 右侧添加提取图标（简化的过滤器符号）

**步骤3：文本预处理**
- 圆角矩形框，浅绿色填充（#E8F5E9），绿色边框（1.5px）
- 内部显示："文本预处理"（16pt，加粗）
- 下方小字："清洗、分词、标准化"（11pt，深灰色）
- 左侧添加处理图标（简化的齿轮符号）

**步骤4：BGE-M3嵌入（核心步骤）**
- 圆角矩形框，浅紫色填充（#F3E5F5），紫色粗边框（2.5px）
- 内部显示："BGE-M3 向量嵌入 ⭐"（18pt，加粗）
- 下方小字："将文本转换为1024维向量"（11pt，深灰色）
- 右侧添加神经网络图标（简化的节点连接符号）
- 框体比其他步骤略宽（宽度增加20%）

**步骤5：FAISS索引构建**
- 圆角矩形框，浅橙色填充（#FFF3E0），橙色边框（1.5px）
- 内部显示："FAISS 索引构建"（16pt，加粗）
- 下方小字："IndexFlatIP 内积索引"（11pt，深灰色）
- 左侧添加索引图标（简化的数据库符号）

**步骤6：向量索引库（最终输出）**
- 圆角矩形框，浅红色填充（#FFEBEE），红色边框（1.5px）
- 内部显示："向量索引库"（16pt，加粗）
- 下方小字："支持快速相似度检索"（11pt，深灰色）
- 右侧添加数据库图标（简化的圆柱体符号）

【连接箭头】
在每两个步骤之间绘制粗黑色实线箭头（3px），垂直向下，箭头旁边标注处理说明：
- 步骤1 → 步骤2：箭头旁标注"解析"（12pt）
- 步骤2 → 步骤3：箭头旁标注"输入"（12pt）
- 步骤3 → 步骤4：箭头旁标注"编码"（12pt）
- 步骤4 → 步骤5：箭头旁标注"向量数据"（12pt）
- 步骤5 → 步骤6：箭头旁标注"存储"（12pt）

【数据流标注】
在流程图右侧添加数据格式说明（浅黄色背景框，虚线边框）：
- 步骤1输出："文本文档"
- 步骤2输出："实体列表"
- 步骤3输出："清洗后文本"
- 步骤4输出："向量矩阵 (N×1024)"
- 步骤5输出："FAISS索引文件"

【布局要求】
- 步骤框宽度约8cm，高度约1.8cm（步骤4略宽）
- 步骤之间垂直间距约1.2cm
- 箭头长度与间距匹配
- 整体居中对齐，数据流标注靠右对齐

【视觉细节】
- 步骤编号使用圆形标记（①②③④⑤⑥），放在框的左上角
- 核心步骤（步骤4）使用星号⭐标记
- 图标使用简化的线条风格，深灰色
- 数据流标注框使用浅黄色背景（#FFFDE7），虚线边框
```

### 图4.3 向量检索与知识图谱协同架构图

**位置**：4.3节末，"向量检索与知识图谱的协同工作机制如图4.3所示"后

**图片标题**：图4.3 向量检索与知识图谱协同架构图

**图片类型**：双路协同架构图

**创建prompt**：
```
请生成一张向量检索与知识图谱协同架构图，展示两种技术的并行协作机制，用于计算机专业毕业论文。

【整体风格】
- 学术论文配图风格，双路并行布局
- 白色背景，清晰的协同关系
- 图片尺寸：宽度15cm，高度11cm，分辨率300dpi
- 所有文字使用中文

【顶部：输入层】
在图的顶部中央绘制：
- 圆角矩形框，浅灰色填充（#F5F5F5），黑色边框（2px）
- 内部显示："用户查询"（18pt，加粗）
- 下方小字："例如：查找所有承重墙实体"（12pt，深灰色）

【中间：双路处理层】
从输入层向下分出两条路径，左右并行：

**左路：向量检索路径（模糊匹配）**
- 路径背景：浅蓝色半透明区域（#E3F2FD，透明度50%）
- 左上角标注："路径A：向量检索"（16pt，加粗，蓝色）

包含3个处理步骤（从上到下）：

1. **查询向量化**
   - 圆角矩形框，白色填充，蓝色边框（1.5px）
   - 显示："查询向量化"（14pt）
   - 小字："BGE-M3 编码"（10pt，深灰色）

2. **FAISS相似度检索**
   - 圆角矩形框，白色填充，蓝色边框（1.5px）
   - 显示："FAISS 相似度检索"（14pt）
   - 小字："Top-K 最相似实体"（10pt，深灰色）

3. **候选实体列表**
   - 圆角矩形框，浅蓝色填充（#BBDEFB），蓝色边框（1.5px）
   - 显示："候选实体列表"（14pt）
   - 小字："IfcWall, IfcWallStandardCase..."（10pt，深灰色）

**右路：知识图谱路径（精确验证）**
- 路径背景：浅绿色半透明区域（#E8F5E9，透明度50%）
- 右上角标注："路径B：知识图谱"（16pt，加粗，绿色）

包含3个处理步骤（从上到下）：

1. **关键词提取**
   - 圆角矩形框，白色填充，绿色边框（1.5px）
   - 显示："关键词提取"（14pt）
   - 小字："NLP 实体识别"（10pt，深灰色）

2. **图谱查询**
   - 圆角矩形框，白色填充，绿色边框（1.5px）
   - 显示："图谱查询"（14pt）
   - 小字："Neo4j Cypher 查询"（10pt，深灰色）

3. **精确实体匹配**
   - 圆角矩形框，浅绿色填充（#C8E6C9），绿色边框（1.5px）
   - 显示："精确实体匹配"（14pt）
   - 小字："属性、关系验证"（10pt，深灰色）

【底部：融合层】
两条路径汇聚到底部：

- 菱形框，浅紫色填充（#F3E5F5），紫色边框（2px）
- 内部显示："结果融合 ⭐"（16pt，加粗）
- 下方小字："加权排序 + 去重"（12pt，深灰色）

【最终输出】
在融合层下方：
- 圆角矩形框，浅红色填充（#FFEBEE），红色边框（2px）
- 内部显示："最终IFC实体映射"（18pt，加粗）
- 下方小字："准确率 > 95%"（12pt，深灰色）

【连接箭头】
- 输入层 → 左路第一步：蓝色实线箭头（2px）
- 输入层 → 右路第一步：绿色实线箭头（2px）
- 左路内部步骤：蓝色实线箭头（2px），垂直向下
- 右路内部步骤：绿色实线箭头（2px），垂直向下
- 左路最后一步 → 融合层：蓝色实线箭头（2px），标注"候选集"
- 右路最后一步 → 融合层：绿色实线箭头（2px），标注"验证结果"
- 融合层 → 最终输出：黑色粗箭头（3px）

【特性标注】
在左路右侧添加标注框（浅蓝色背景，虚线边框）：
- "优势：召回率高"
- "特点：模糊匹配"

在右路左侧添加标注框（浅绿色背景，虚线边框）：
- "优势：准确率高"
- "特点：精确验证"

【布局要求】
- 输入层宽度约10cm，居中
- 左右两路各占画面40%宽度，中间留白
- 每个处理步骤框宽度约5cm，高度约1.5cm
- 融合层宽度约8cm，居中
- 最终输出宽度约10cm，居中

【视觉细节】
- 使用不同颜色区分两条路径（蓝色 vs 绿色）
- 融合层使用星号⭐标记，表示关键步骤
- 路径背景使用半透明色块，增强视觉分组
- 特性标注框使用虚线边框，与主流程区分
```

### 表4.1 IDS生成管道各阶段对比表

**预估内容**：阶段名称、输入、输出、核心任务、关键技术

### 表4.2 Facet类型分类表

**预估内容**：六种Facet类型（EntityFacet、PartOfFacet等）、适用场景、示例

### 表4.3 向量检索性能对比表

**预估内容**：不同索引类型（IndexFlatIP vs IndexIVFFlat）的检索时间、召回率

---

## 三、第五章图表需求（预估：2图+3表）

### 图5.1 系统前端界面截图

**预估内容**：登录页、IDS生成页、审查结果页的界面截图（需实际系统运行截图）

### 图5.2 性能测试结果柱状图/折线图

**预估内容**：响应时间分布、并发能力曲线

### 表5.1 性能测试结果汇总表

**预估内容**：测试场景、响应时间、成功率、吞吐量

### 表5.2 IDS生成质量评估表

**预估内容**：测试样本数、准确率、召回率、F1分数

### 表5.3 与现有方法对比表

**预估内容**：本研究 vs Guo等(2025) vs Madireddy等(2025)的指标对比

---

## 四、图表创建通用规范

### 4.1 图片规范

| 规范项 | 要求 |
|--------|------|
| 图片尺寸 | 宽度不超过16cm，高度不超过12cm |
| 图片格式 | PNG（推荐）或矢量图（SVG/PDF） |
| 图片编号 | 按章节编号（图3.1、图4.1等） |
| 图片标题 | 置于图片下方，居中，格式为"图X.X 标题" |
| 图片分辨率 | 至少300dpi，确保打印清晰 |
| 图片来源 | 自绘图片无需标注来源；引用图片需标注出处 |

### 4.2 表格规范

| 规范项 | 要求 |
|--------|------|
| 表格格式 | 三线表（顶线粗、栏目线细、底线粗） |
| 表格编号 | 按章节编号（表3.1、表4.1等） |
| 表格标题 | 置于表格上方，居中，格式为"表X.X 标题" |
| 表头样式 | 加粗居中 |
| 内容对齐 | 文字左对齐，数字右对齐或居中 |
| 表格宽度 | 不超过页面宽度，必要时跨页 |

### 4.3 正文引用要求

在正文中引用图表时：
- 图片："如图X.X所示"、"见图X.X"
- 表格："如表X.X所示"、"详见表X.X"
- 确保图表编号与引用一致

---

## 五、图表文件存放位置

创建后的图表文件存放路径：
- 图片文件：`essay/figures/` 目录，命名格式 `figure_X_X.png`
- 表格文件：直接嵌入Markdown，或存放于 `essay/tables/` 目录（如需Excel源文件）
- 此指引文档：`essay/chapters/FIGURE_AND_TABLE_CONTENT_GUIDE.md`

---

## 六、图表创建工具推荐

| 工具 | 适用场景 | 备注 |
|------|----------|------|
| Draw.io | 流程图、架构图、框图 | 免费、在线/离线均可 |
| Visio | 专业流程图、架构图 | 需Microsoft Office |
| PowerPoint | 简单框图、层级图 | 可导出为图片 |
| PlantUML | UML序列图 | 文本生成，需渲染工具 |
| Mermaid | 流程图、序列图 | Markdown内嵌，需渲染 |
| Python Matplotlib | 数据图表（柱状图、折线图） | 第五章性能测试数据可视化 |
| Excel | 表格数据整理 | 可导出为图片 |

---

**【第三章图表需求汇总】**

| 图表编号 | 名称 | 类型 | 位置 |
|----------|------|------|------|
| 图3.1 | 系统功能模块关系图 | 流程图 | 3.1.1节末 |
| 图3.2 | 系统整体架构图 | 架构图 | 3.2.1节末 |
| 图3.3 | IDS生成请求前后端交互序列图 | UML序列图 | 3.2.3节末 |
| 图3.4 | MongoDB数据存储结构示意图 | 数据结构图 | 3.2.4节末 |
| 表3.1 | 用户角色功能权限矩阵 | 表格 | 3.1.3节中 |
| 表3.2 | 系统技术选型汇总 | 表格 | 3.3节开头 |

**共计：4张图 + 2个表格**