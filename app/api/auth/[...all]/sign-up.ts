//import { email } from "better-auth";
import { authClient } from "../../../lib/auth-client";

// This is a test file for sign-up functionality.
// In production, sign-up should be handled by the frontend form.

// Example usage (do not use in production):
// const { data, error } = await authClient.signUp.email({
//   email: "user@example.com",
//   password: "your-password",  // at least 8 characters
//   name: "Your Name",
//   callbackURL: "/dashboard",
// });

// For production, implement sign-up via the frontend using better-auth's signUp.email() method.
